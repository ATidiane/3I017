/**
 * 
 */

function test(fromId) {
	env.msgs = [];
	/*env.minId = -1;
	env.maxId = -1;
	
	alert(fromId);
	
	if(fromId == undefined) {
		fromId = -1;
	}
	
	env.fromId = fromId;
	env.fromLogin = fromLogin;
	
	console.log(env.fromLogin);
	env.query = query;
	
	alert(env.fromId);*/
	
	//var s = "<div id=\'1\'>Test de la page" + a + "</div>";
	//document.body.innerHTML = s;
}